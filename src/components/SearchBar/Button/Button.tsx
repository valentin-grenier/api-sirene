import { BsSearch } from 'react-icons/bs';
import './Button.scss';

const Button = () => {
  return (
    <>
      <button type="submit">
        <BsSearch size={14} />
      </button>
    </>
  );
};

export default Button;

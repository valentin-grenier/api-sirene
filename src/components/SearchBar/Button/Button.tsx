import { BsSearch } from 'react-icons/bs';
import './Button.scss';

const Button = () => {
  return (
    <>
      <button type="submit">
        <BsSearch />
      </button>
    </>
  );
};

export default Button;
